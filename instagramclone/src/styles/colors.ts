const darkTheme = false;

const colors = {
  primary: darkTheme ? '#000' : '#fff',
  headerBackground: darkTheme ? '#000' : '#f5f5f5',
  iconColor: darkTheme ? '#fff' : '#000',
};

export default colors;
